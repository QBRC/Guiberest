<web-app id="WebApp_ID" version="2.4"
	xmlns="http://java.sun.com/xml/ns/j2ee" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee 
	http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">
	
	<display-name>Guiberest</display-name>

    <!--
         This Guice listener hijacks all further filters and servlets. Extra
         filters and servlets have to be configured in your
         ServletModule#configureServlets() by calling
         serve(String).with(Class<? extends HttpServlet>) and
         filter(String).through(Class<? extends Filter)
     -->
     <!-- 
    <listener>
        <listener-class>edu.swmed.qbrc.guiberest.guice.GuiceServletConfig</listener-class>
    </listener>

    <filter>
        <filter-name>guiceFilter</filter-name>
        <filter-class>com.google.inject.servlet.GuiceFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>guiceFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
 -->
 
	<context-param>
        <param-name>resteasy.guice.modules</param-name>
        <param-value>edu.swmed.qbrc.guiberest.guice.RestModule</param-value>
    </context-param>
 
    <listener>
        <listener-class>
            org.jboss.resteasy.plugins.guice.GuiceResteasyBootstrapServletContextListener
        </listener-class>
    </listener>

    <servlet>
        <servlet-name>resteasy-servlet</servlet-name>
        <servlet-class>
            org.jboss.resteasy.plugins.server.servlet.HttpServletDispatcher
        </servlet-class>
    </servlet>

	<servlet-mapping>
		<servlet-name>resteasy-servlet</servlet-name>
		<url-pattern>/*</url-pattern>
	</servlet-mapping>
	
	<!-- If the resteasy-servlet servlet mapping is something other than /*, we need to specify the URL here. -->
	<!--
	<context-param>
		<param-name>resteasy.servlet.mapping.prefix</param-name>
		<param-value>/</param-value>
	</context-param>
	-->
	
	<!-- Configure the interceptor for authentication -->
	<!-- Configure Custom Jackson JSON (de)Serializers -->
	<context-param>
		<param-name>resteasy.providers</param-name>
		<param-value>edu.swmed.qbrc.auth.cashmac.server.ValidationInterceptorCasHmac,edu.swmed.qbrc.guiberest.shared.rest.jackson.JacksonConfigProvider</param-value>
	</context-param>


	<!-- The URL where users go to log in to your CAS server -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.cas.serverLoginUrl</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.cas.serverLoginUrl}</param-value>
	</context-param>
	<!-- The base URL of your RESTful service.  This URL is used by your CAS server to
	     identify your application. -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.cas.serviceName</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.cas.serviceName}</param-value>
	</context-param>
	
	<!-- Context parameters for HMAC server -->
	<!-- The name of the table that stores Role information -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.Role</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.Role}</param-value>
	</context-param>
	<!-- The name of the column that uniquely identifies the role in the Roles table -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.keycol.Role</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.keycol.Role}</param-value>
	</context-param>	
	<!-- The name of the column that identifies the user in the Roles table -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.usercol.Role</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.usercol.Role}</param-value>
	</context-param>
	<!-- The name of the column that names the role in the Roles table
	     NOTE: There can be multiple rows in the Roles table for each user-->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.rolecol.Role</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.rolecol.Role}</param-value>
	</context-param>
	<!-- The name of the table that stores user information -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.User</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.User}</param-value>
	</context-param>
	<!-- The name of the column that uniquely identifies each user in the Users table -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.keycol.User</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.keycol.User}</param-value>
	</context-param>
	<!-- The name of the column that contains the secret HMAC key for the user in the Users table -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.table.secretCol.User</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.table.secretCol.User}</param-value>
	</context-param>
	<!-- The class of the JDBC driver to use -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.jdbcdriver</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.jdbcdriver}</param-value>
	</context-param>
	<!-- The URL for the JDBC driver to use to connect to your database. -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.jdbcurl</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.jdbcurl}</param-value>
	</context-param>
	<!-- The user name to use to access your database. -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.username</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.username}</param-value>
	</context-param>
	<!-- The password to use to access your database. -->
	<context-param>
		<param-name>edu.swmed.qbrc.auth.cashmac.hmac.password</param-name>
		<param-value>${edu.swmed.qbrc.auth.cashmac.hmac.password}</param-value>
	</context-param>
      	
	<!-- CAS Filters -->
	<filter>
	   <filter-name>CAS Single Sign Out Filter</filter-name>
	   <filter-class>org.jasig.cas.client.session.SingleSignOutFilter</filter-class>
	</filter>
	
	<!-- Purposefully skipped the "CAS Authentication Filter" since we handle it in Java code -->
	
	<filter>
		<filter-name>CAS Validation Filter</filter-name>
		<filter-class>org.jasig.cas.client.validation.Cas20ProxyReceivingTicketValidationFilter</filter-class>
		<init-param>
			<param-name>casServerUrlPrefix</param-name>
			<param-value>${cas.casserverurlprefix}</param-value>
		</init-param>
		<init-param>
			<param-name>serverName</param-name>
			<param-value>${cas.servername}</param-value>
		</init-param>
		<init-param>
			<param-name>proxyCallbackUrl</param-name>
			<param-value>${cas.proxycallbackurl}</param-value>
		</init-param>
		<init-param>
			<param-name>proxyReceptorUrl</param-name>
			<param-value>${cas.proxyreceptorurl}</param-value>
		</init-param>
	</filter>
	
	<filter>
		<filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
		<filter-class>org.jasig.cas.client.util.HttpServletRequestWrapperFilter</filter-class>
	</filter>
	
	<filter>
		<filter-name>CAS Assertion Thread Local Filter</filter-name>
		<filter-class>org.jasig.cas.client.util.AssertionThreadLocalFilter</filter-class>
	</filter>


	<filter-mapping>
	   <filter-name>CAS Single Sign Out Filter</filter-name>
	   <url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<!-- Purposefully skipped the "CAS Authentication Filter" since we handle it in Java code -->

	<filter-mapping>
		<filter-name>CAS Validation Filter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	 
	<filter-mapping>
		<filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<filter-mapping>
		<filter-name>CAS Assertion Thread Local Filter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<filter-mapping>
		<filter-name>CAS Validation Filter</filter-name>
		<url-pattern>/proxyCallback</url-pattern>	
	</filter-mapping>
	
	<listener>
	    <listener-class>org.jasig.cas.client.session.SingleSignOutHttpSessionListener</listener-class>
	</listener>
	
 
</web-app>

